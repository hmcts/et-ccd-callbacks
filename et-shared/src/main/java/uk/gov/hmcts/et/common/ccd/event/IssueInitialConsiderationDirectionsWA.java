package uk.gov.hmcts.et.common.ccd.event;

import java.util.Set;
import org.springframework.stereotype.Component;
import uk.gov.hmcts.ccd.sdk.api.CCDConfig;
import uk.gov.hmcts.ccd.sdk.api.ConfigBuilder;
import uk.gov.hmcts.ccd.sdk.api.Permission;
import uk.gov.hmcts.et.common.model.ccd.CaseData;
import uk.gov.hmcts.et.common.model.ccd.State;
import uk.gov.hmcts.et.common.model.ccd.UserRole;

/**
 * Generated by ccd-definition-converter from ET_EnglandWales on migration; owned by this service team.
 *
 * <p>Event {@code issueInitialConsiderationDirectionsWA} for case type {@code ET_EnglandWales}.
 */
@Component
public class IssueInitialConsiderationDirectionsWA implements CCDConfig<CaseData, State, UserRole> {
    /**
     * The CCD event ID.
     */
    public static final String ISSUE_INITIAL_CONSIDERATION_DIRECTIONS_WA = "issueInitialConsiderationDirectionsWA";

    /**
     * Registers event {@code issueInitialConsiderationDirectionsWA}.
     *
     * @param builder the config builder
     */
    @Override
    public void configure(ConfigBuilder<CaseData, State, UserRole> builder) {
        var fields = builder.event(ISSUE_INITIAL_CONSIDERATION_DIRECTIONS_WA)
            .forState(State.Accepted)
            .name("Issue IC Directions WA")
            .showSummary(false)
            .publishToCamunda()
            .explicitGrants()
            .grant(Permission.CRU, UserRole.CASEWORKER_EMPLOYMENT_API, UserRole.CASEWORKER_EMPLOYMENT_ENGLANDWALES)
            .grant(Set.of(Permission.R), UserRole.CASEWORKER_WA_TASK_CONFIGURATION)
            .fields();
        fields.page("1");
        fields.pageLabel(" ");
        fields.readonly(CaseData::getHorizontalLine)
                    .publish(false);
        fields.optionalNoSummary(CaseData::getNextListedDate)
                    .fieldShowCondition("horizontalLine=\"dummy\"");
    }
}
