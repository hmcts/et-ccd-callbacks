package uk.gov.hmcts.et.common.ccd.event;

import org.springframework.stereotype.Component;
import uk.gov.hmcts.ccd.sdk.api.CCDConfig;
import uk.gov.hmcts.ccd.sdk.api.ConfigBuilder;
import uk.gov.hmcts.ccd.sdk.api.Permission;
import uk.gov.hmcts.et.common.model.ccd.CaseData;
import uk.gov.hmcts.et.common.model.ccd.State;
import uk.gov.hmcts.et.common.model.ccd.UserRole;

/**
 * Generated by ccd-definition-converter from ET_EnglandWales on migration; owned by this service team.
 *
 * <p>Event {@code SUBMIT_CASE_DRAFT} for case type {@code ET_EnglandWales}.
 */
@Component
public class SUBMITCASEDRAFT implements CCDConfig<CaseData, State, UserRole> {
    /**
     * The CCD event ID.
     */
    public static final String SUBMIT_CASE_DRAFT = "SUBMIT_CASE_DRAFT";

    /**
     * Registers event {@code SUBMIT_CASE_DRAFT}.
     *
     * @param builder the config builder
     */
    @Override
    public void configure(ConfigBuilder<CaseData, State, UserRole> builder) {
        var fields = builder.event(SUBMIT_CASE_DRAFT)
            .forStateTransition(State.AWAITING_SUBMISSION_TO_HMCTS, State.Submitted)
            .name("Submit Draft")
            .description("Submit a draft case")
            .displayOrder(6)
            .showSummary(false)
            .publishToCamunda()
            .significant()
            .ttlIncrement(36524)
            .explicitGrants()
            .grant(Permission.CRU, UserRole.CASEWORKER_WA_TASK_CONFIGURATION)
            .grant(Permission.CRUD, UserRole.CASEWORKER_EMPLOYMENT_API, UserRole.CREATOR)
            .fields();
        fields.page("1");
        fields.pageLabel(" ");
        fields.readonly(CaseData::getHorizontalLine)
                    .publish(false);
        fields.optionalNoSummary(CaseData::getNextListedDate)
                    .fieldShowCondition("horizontalLine=\"dummy\"");
    }
}
