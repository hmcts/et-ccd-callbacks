package uk.gov.hmcts.et.common.ccd.event;

import java.util.EnumSet;
import org.springframework.stereotype.Component;
import uk.gov.hmcts.ccd.sdk.api.CCDConfig;
import uk.gov.hmcts.ccd.sdk.api.ConfigBuilder;
import uk.gov.hmcts.ccd.sdk.api.Permission;
import uk.gov.hmcts.et.common.model.ccd.CaseData;
import uk.gov.hmcts.et.common.model.ccd.State;
import uk.gov.hmcts.et.common.model.ccd.UserRole;

/**
 * Generated by ccd-definition-converter from ET_EnglandWales on migration; owned by this service team.
 *
 * <p>Event {@code MarkForDisposal} for case type {@code ET_EnglandWales}.
 */
@Component
public class MarkForDisposal implements CCDConfig<CaseData, State, UserRole> {
    /**
     * The CCD event ID.
     */
    public static final String MARK_FOR_DISPOSAL = "MarkForDisposal";

    /**
     * Registers event {@code MarkForDisposal}.
     *
     * @param builder the config builder
     */
    @Override
    public void configure(ConfigBuilder<CaseData, State, UserRole> builder) {
        builder.event(MARK_FOR_DISPOSAL)
            .forStateTransition(EnumSet.of(State.AWAITING_SUBMISSION_TO_HMCTS, State.Delete), State.PendingDisposal)
            .name("Mark for disposal")
            .description("Mark an expired draft case for disposal")
            .displayOrder(1001)
            .showSummary(false)
            .significant()
            .explicitGrants()
            .grant(Permission.CRU, UserRole.CASEWORKER_EMPLOYMENT_API);
    }
}
